# 开发环境
spring:
  datasource:
    url: jdbc:p6spy:mysql://localhost:3306/afang?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Mysql123456
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver

    # redis配置
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 10000ms
      client-type: lettuce
      lettuce:
        pool:
          max-active: 8
          max-wait: -1ms
          max-idle: 8
          min-idle: 0
  # 邮箱配置
  mail:
    host: smtp.qq.com
    port: 465
    username: 1874705872@qq.com
#    TODO:换自己的
    password: jowebcqlzgrhceib
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls.enable: true
          socketFactory.class: javax.net.ssl.SSLSocketFactory

# mybatis-plus配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: assign_id
server:
  port: 9999
  servlet:
    context-path: /api

# jwt配置
jwt:
  secret: jwtmw

# 日志配置
logging:
  level:
    org.springframework: info
    org.mybatis: info